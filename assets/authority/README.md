# Naming authority records

VOResource metadata for IVOA naming authorities served in Benson’s own OAI catalog.

| File | Role |
|------|------|
| `ivoa.xml` | `vg:Authority` record for `ivo://ivoa.net` |

Loaded by [`src/benson/registry/registry_self.py`](../../src/benson/registry/registry_self.py) when building RofR ListRecords output.

Full context: [`docs/schemas-and-validation-assets.md`](../../docs/schemas-and-validation-assets.md).
